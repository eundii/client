import styled from "@emotion/styled";

const BodyWrap = styled.div`
  max-width: 128rem;
  padding: 2rem;
  margin: 0 auto;
`;

const SectionWrap = styled.section`
  max-width: 80rem;
  margin: 0 auto;
`;

const SectionTitle = styled.h2`
  margin: 2rem 0;
  font-size: 2.8rem;
  font-weight: bold;
  text-align: left;
`;

const SectionInner = styled.div`
  margin-bottom: 5rem;
`;

export { BodyWrap, SectionWrap, SectionTitle, SectionInner };
